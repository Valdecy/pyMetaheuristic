from setuptools import setup, find_packages

setup(
    name="pyMetaheuristic",
    version="0.0.1",
    author="",
    author_email="",
    packages=find_packages(),
    install_requires=[
        'matplotlib',
        'numpy',
        'scipy'
    ],
)
